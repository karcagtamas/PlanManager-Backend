<div class="sl-movie-selector-dialog">
    @if (IsLoading)
    {
        <Loader></Loader>
    }
    else
    {
        @if (List != null && List.Count > 0)
        {
            <div class="movie-list">
                <ListTable Header="Header" Body="List" TList="ManagerAPI.Shared.DTOs.SL.MyMovieSelectorListDto" IsSelectionEnabled="true" SelectedIndexes="SelectedIndexList" FooterDisplay="false" OnRowClick="@(e => SwitchMineFlag(e))"></ListTable>
            </div>
        }
    }
</div>