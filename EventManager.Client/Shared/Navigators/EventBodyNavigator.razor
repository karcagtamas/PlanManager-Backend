@inject EventManager.Client.Services.Interfaces.IHelperService HelperService

<NavMenu>
    <hr />
    <MatNavMenu>
        <AuthorizeView>
            <Authorized>
                <MatNavItem Href="@GetPath("data")">Data</MatNavItem>
                <MatNavItem Href="@GetPath("notifications")">Notifications</MatNavItem>
            </Authorized>
        </AuthorizeView>
    </MatNavMenu>
</NavMenu>
    


@code {

    [Parameter]
    public int Id { get; set; }

    private string GetPath(string page)
    {
        return $"/events/{Id}/{page}";
    }
}